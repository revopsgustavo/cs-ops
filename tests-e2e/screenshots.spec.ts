import {mkdir} from 'node:fs/promises';import path from 'node:path';import {test} from '@playwright/test';
test('gera screenshots versionáveis desktop e mobile',async({page},testInfo)=>{test.skip(testInfo.project.name!=='desktop');const dir=path.resolve('docs/screenshots');await mkdir(dir,{recursive:true});await page.setViewportSize({width:1440,height:900});await page.goto('/');await page.screenshot({path:path.join(dir,'cockpit-desktop.png'),fullPage:true});await page.goto('/health-score');await page.screenshot({path:path.join(dir,'health-score-desktop.png'),fullPage:true});await page.setViewportSize({width:375,height:812});await page.goto('/customer-360');await page.screenshot({path:path.join(dir,'customer-360-mobile.png'),fullPage:true})});
