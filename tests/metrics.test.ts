import {describe,expect,it} from 'vitest';import {accounts,revenueMovements,AS_OF_DATE} from '../src/data';import {daysUntil,expansionEligible,mrrAtRisk,retention} from '../src/rules';
describe('reconciliação executiva',()=>{it('reconcilia métricas com oráculos independentes e a data-base',()=>{const r=retention(revenueMovements),total=accounts.reduce((s,a)=>s+a.mrr,0),eligible=accounts.filter(expansionEligible),top5=[...accounts].sort((a,b)=>b.mrr-a.mrr).slice(0,5).reduce((s,a)=>s+a.mrr,0),categories=Object.fromEntries(['Crítico','Risco','Atenção','Saudável'].map(k=>[k,accounts.filter(a=>a.risk===k).length]));const result={asOf:AS_OF_DATE,accounts:accounts.length,mrr:total,arr:total*12,mrrAtRisk:mrrAtRisk(accounts),expansionPotential:eligible.reduce((s,a)=>s+a.expansionPotential,0),eligibleAccounts:eligible.length,grr:+r.grr.toFixed(2),nrr:+r.nrr.toFixed(2),logoChurn:+(revenueMovements.filter(x=>x.churn>0).length/revenueMovements.length*100).toFixed(2),revenueChurn:+(r.churn/r.opening*100).toFixed(2),contraction:r.contraction,expansion:r.expansion,top5Concentration:+(top5/total*100).toFixed(2),averageHealth:+(accounts.reduce((s,a)=>s+a.health,0)/accounts.length).toFixed(2),categories,renewals30:accounts.filter(a=>daysUntil(a.renewalDate)<=30).length,renewals60:accounts.filter(a=>daysUntil(a.renewalDate)<=60).length,renewals90:accounts.filter(a=>daysUntil(a.renewalDate)<=90).length,withoutAction:accounts.filter(a=>!a.nextAction).length,nextActionCompleteness:+(accounts.filter(a=>a.nextAction).length/accounts.length*100).toFixed(2)};expect(result).toEqual({asOf:'2026-08-13',accounts:36,mrr:393570,arr:4722840,mrrAtRisk:100598,expansionPotential:8749,eligibleAccounts:3,grr:97.19,nrr:98.87,logoChurn:8.33,revenueChurn:1.92,contraction:3438,expansion:5567,top5Concentration:25.01,averageHealth:63.97,categories:{Crítico:4,Risco:7,Atenção:22,Saudável:3},renewals30:2,renewals60:6,renewals90:9,withoutAction:4,nextActionCompleteness:88.89});expect(accounts.every(a=>a.arr===a.mrr*12)).toBe(true);expect(r.grr).toBeLessThanOrEqual(100)})});
